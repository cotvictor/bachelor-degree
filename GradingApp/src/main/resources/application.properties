spring.output.ansi.enabled=ALWAYS
spring.jpa.generate-ddl=true
spring.jpa.hibernate.ddl-auto=create-drop

server.port = 9090

# avoid exceptions for those entities that are loaded lazy and serialization happens before they get fully loaded
spring.jackson.serialization.fail-on-empty-beans=false


spring.datasource.url=jdbc:mysql://localhost:3306/gradeappDB
spring.datasource.driver-class-name=com.mysql.jdbc.Driver
spring.datasource.username=touristicGO
spring.datasource.password=OGcitsiruot

#logging.level.=DEBUG